# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Supabase CLI local cache
supabase/.temp/
.aso-venv/

# Local env files (staging/prod credentials live in Vercel, not the repo)
.env
.env.*
!.env.example

# test output
test-results/
playwright-report/
e2e-screens/

# Xcode per-user UI state: which files were open, where the cursor was. It is not code, it
# changes on every build, and it has been landing in commits about the app itself.
xcuserdata/
